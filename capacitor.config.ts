import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'RAGAIA-Front',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
